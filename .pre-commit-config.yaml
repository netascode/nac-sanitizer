repos:
  - repo: local
    hooks:
      - id: license-headers
        name: license headers (SPDX)
        entry: bash scripts/license-headers.sh
        language: system
        types: [python]
        pass_filenames: false

      - id: ruff-lint
        name: ruff (lint)
        entry: ruff check --fix
        language: system
        types: [python]

      - id: ruff-format
        name: ruff (format)
        entry: ruff format
        language: system
        types: [python]

      - id: ty
        name: ty (type check)
        entry: ty check
        language: system
        types: [python]
        pass_filenames: false

      - id: bandit
        name: bandit (security)
        entry: bandit -c pyproject.toml
        language: system
        types: [python]
